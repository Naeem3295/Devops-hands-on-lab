Tasks:
Launch a t2.micro EC2 instance (Ubuntu 22.04) in AWS Console.
Create a Security Group with ports 22, 80, and 443 open. (SSH only from your IP)
Connect to the instance via SSH.
Install Nginx and change the default page to "Hello, I am learning!".
Create a custom site configuration (any name).
Create a hosted zone in Route53 and add an A record (use your domain or free domain).
Install SSL certificate using Certbot.
Set up a cronjob to backup /var/log/nginx/ daily.
Take screenshots of all config files.
Make the website live and open it in browser with HTTPS.

